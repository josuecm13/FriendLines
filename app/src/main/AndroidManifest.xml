<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.friendlines">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.profile.GalleryImageActivity"></activity>
        <activity android:name=".view.auth.RecoverPasswordActivity" />
        <activity android:name=".view.profile.EducationActivity" />
        <activity android:name=".view.profile.EditProfileActivity" />
        <activity android:name=".view.auth.register.RegBirthGenderActivity" />
        <activity android:name=".view.auth.register.RegNamesActivity" />
        <activity android:name=".view.auth.register.RegisterActivity" />
        <activity android:name=".view.auth.LoginActivity" />
        <activity
            android:name=".view.LaunchActivity"
            android:theme="@style/NoActionBar" />
        <activity android:name=".view.MainActivity" />
        <activity
            android:name=".view.SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>